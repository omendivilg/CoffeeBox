# Directorios de dependencias
node_modules/

# Directorios de build y output de Next.js
.next/
out/

# Archivos de configuración de entorno (sensibles y locales)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos de caché y configuración de Firebase local
.firebase/
.firebaserc # Aunque este a veces se versiona si el equipo lo comparte, es común ignorarlo si solo tiene configuraciones locales.

# Logs
npm-debug.log*
yarn-debug.log*
*.log

# macOS
.DS_Store

# Windows
Thumbs.db

# VS Code
.vscode/ # Ignora la configuración local de VS Code. Puedes omitir esta línea si tu equipo comparte configuraciones específicas.

# TypeScript
*.tsbuildinfo

# Cobertura de pruebas (si usas Jest, Vitest, etc.)
coverage/

# Archivos de empaquetado (ej. si generas un .zip de tu build)
*.zip
*.tar.gz

# Archivos de base de datos o datos sensibles que puedan generarse
# Si tienes una base de datos local como SQLite, podrías añadirla aquí
# *.sqlite
# /data/*.db

# Archivos generados por pnpm (si no quieres versionar el lockfile, pero generalmente sí quieres)
# Si por alguna razón no quieres pnpm-lock.yaml versionado, lo añadirías aquí
# pnpm-lock.yaml